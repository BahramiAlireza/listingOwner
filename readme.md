As the project was not that big i didn't dockerize it. instead i wrote a bash script to run the server.

you can run the server using the command below in the **project root directory**:

```bash
>> bash runserver.sh
```